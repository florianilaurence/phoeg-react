import {useState} from "react";


export default function NavBar() {
    const [state, setState] = useState({ isOpen: false });
    let toggle = () => setState({ isOpen: !this.state.isOpen })

    return (
        <div className="mobile-wrapper" >

        </div>
    )

}